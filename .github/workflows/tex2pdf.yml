name: Compile LaTeX and Commit PDF

on:
  push:
    paths:
      - 'CV/*.tex'
  schedule:
    - cron: '0 0 * * *'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 🔄 Checkout with push access
    - name: Checkout Repository
      uses: actions/checkout@v4
      with:
        persist-credentials: true
        fetch-depth: 0

    # 📄 Compile the LaTeX file
    - name: Compile LaTeX
      uses: xu-cheng/latex-action@v3
      with:
        root_file: CV/aradfarahani.tex

    # 📦 Upload the PDF so you can download it from Actions
    - name: Upload PDF Artifact
      uses: actions/upload-artifact@v4
      with:
        name: cv-pdf
        path: CV/aradfarahani.pdf

    # 🕒 Generate timestamp
    - name: Set Timestamp
      run: echo "TIMESTAMP=$(date +'%Y-%m-%d %H:%M:%S')" >> $GITHUB_ENV

    # ✅ Commit and push the PDF back to repository
    - name: Commit PDF
      uses: EndBug/add-and-commit@v9
      with:
        author_name: GitHub Actions
        author_email: actions@github.com
        message: "Update CV PDF – ${{ env.TIMESTAMP }}"
        add: 'CV/aradfarahani.pdf'
